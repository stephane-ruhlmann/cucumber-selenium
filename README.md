# cucumber-selenium-poc
A simple cucumber bootstrap for testing on a selenium server

## Install
```
npm install
```

## Run the tests
Launch a local Selenium server or provide host, port and access credentials through env variables, then :

```
npm test
```
